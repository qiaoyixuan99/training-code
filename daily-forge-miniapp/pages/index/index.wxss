/* ──────────────────────────────────────
   Daily Forge — 主页样式
   CSS 变量定义在 .app 容器上实现动态主题切换
   ────────────────────────────────────── */

/* ═══ 主题变量（黑夜模式 — 默认） ═══ */
.app {
  --bg: #0f1117;
  --surface: #1a1d27;
  --surface2: #22263a;
  --border: #2a2e3f;
  --text: #e4e6f0;
  --text2: #9a9db5;
  --text3: #636883;
  --gold: #f0b90b;
  --gold2: #c99400;
  --xp: #00d4aa;
  --xp2: #009e7e;
  --streak: #ff6b6b;
  --morning: #f0a050;
  --noon: #ffd700;
  --afternoon: #60b0f0;
  --evening: #7c5ce7;
  --danger: #e0556a;
  --radius: 28rpx;
  --radius-sm: 20rpx;

  min-height: 100vh;
  background: var(--bg);
  color: var(--text);
  padding-bottom: 80rpx;
}

/* ═══ 主题变量（白天模式） ═══ */
.app.light-theme {
  --bg: #f5f5f7;
  --surface: #ffffff;
  --surface2: #f0f0f5;
  --border: #e0e0e6;
  --text: #1d1d26;
  --text2: #6e6e80;
  --text3: #9a9ab0;
  --gold: #c99400;
  --gold2: #a07800;
  --xp: #009e7e;
  --xp2: #007a60;
  --streak: #e04545;
  --morning: #d07020;
  --noon: #c9a000;
  --afternoon: #3078c0;
  --evening: #5a3ec8;
  --danger: #c04050;
}

/* 白天模式下的特别覆盖 */
.app.light-theme .badge-morning { background: #fef0e0; }
.app.light-theme .badge-noon { background: #fff8dc; }
.app.light-theme .badge-afternoon { background: #e0eefc; }
.app.light-theme .badge-evening { background: #e8e0fa; }
.app.light-theme .task-card.completed { background: #eafaf4; border-color: var(--xp); }
.app.light-theme .week-day.done { background: #eafaf4; }
.app.light-theme .identity-banner { background: linear-gradient(135deg, #1a1d30 0%, #1d2035 100%); }
.app.light-theme .identity-banner { background: linear-gradient(135deg, #f8f7ff 0%, #f0eff8 100%); }

/* ═══ 自定义导航栏 ═══ */
.nav-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx 32rpx;
  padding-top: calc(88rpx + env(safe-area-inset-top));
}

.nav-date {
  font-size: 26rpx;
  color: var(--text3);
  display: block;
}

.nav-title {
  font-size: 38rpx;
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--text);
}

.gold { color: var(--gold); }

/* ═══ 主题切换 ═══ */
.theme-toggle {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  padding: 14rpx 28rpx;
  border-radius: 32rpx;
  background: var(--surface);
  border: 2rpx solid var(--border);
  color: var(--text2);
  font-size: 24rpx;
  transition: all 0.2s;
}

.theme-toggle:active {
  transform: scale(0.95);
  border-color: var(--gold);
  color: var(--gold);
}

/* ═══ 统计卡片 ═══ */
.stats-row {
  display: flex;
  gap: 16rpx;
  padding: 0 32rpx;
  margin-bottom: 28rpx;
}

.stat-card {
  flex: 1;
  background: var(--surface);
  border: 2rpx solid var(--border);
  border-radius: var(--radius-sm);
  padding: 24rpx;
  text-align: center;
}

.stat-value {
  font-size: 44rpx;
  font-weight: 700;
  display: block;
}

.stat-label {
  font-size: 22rpx;
  color: var(--text3);
  margin-top: 6rpx;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  display: block;
}

.stat-card.streak .stat-value { color: var(--streak); }
.stat-card.xp .stat-value { color: var(--xp); }
.stat-card.level .stat-value { color: var(--gold); }

/* ═══ XP 进度条 ═══ */
.xp-section {
  padding: 0 32rpx;
  margin-bottom: 28rpx;
}

.xp-header {
  display: flex;
  justify-content: space-between;
  font-size: 24rpx;
  color: var(--text2);
  margin-bottom: 10rpx;
}

.xp-bar-outer {
  height: 14rpx;
  background: var(--surface2);
  border-radius: 7rpx;
  overflow: hidden;
}

.xp-bar-inner {
  height: 100%;
  background: linear-gradient(90deg, var(--xp2), var(--xp));
  border-radius: 7rpx;
  transition: width 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
}

/* ═══ 身份横幅 ═══ */
.identity-banner {
  margin: 0 32rpx 28rpx;
  background: linear-gradient(135deg, #1a1d30 0%, #1d2035 100%);
  border: 2rpx solid var(--border);
  border-radius: var(--radius);
  padding: 28rpx 32rpx;
  text-align: center;
}

.identity-title {
  font-size: 22rpx;
  color: var(--text3);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  display: block;
}

.identity-name {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--gold);
  margin: 8rpx 0;
  display: block;
}

.identity-sub {
  font-size: 24rpx;
  color: var(--text2);
}

/* ═══ 区块标题 ═══ */
.section-title {
  font-size: 24rpx;
  color: var(--text3);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: 18rpx;
  display: block;
  padding: 0 32rpx;
}

.section-gap {
  margin-top: 36rpx;
}

/* ═══ 任务卡片 ═══ */
.task-card {
  margin: 0 32rpx 20rpx;
  background: var(--surface);
  border: 2rpx solid var(--border);
  border-radius: var(--radius);
  padding: 28rpx;
  transition: all 0.25s;
  position: relative;
}

.task-card.completed {
  border-color: var(--xp);
  background: #0f1a17;
}

/* 已完成勾号 */
.task-card.completed::after {
  content: '✓';
  position: absolute;
  top: 24rpx;
  right: 32rpx;
  font-size: 40rpx;
  color: var(--xp);
  font-weight: 700;
}

.task-time-badge {
  display: inline-block;
  padding: 6rpx 20rpx;
  border-radius: 32rpx;
  font-size: 22rpx;
  font-weight: 600;
  margin-bottom: 14rpx;
  letter-spacing: 0.03em;
}

.badge-morning { background: #3a2a14; color: var(--morning); }
.badge-noon { background: #3a3510; color: var(--noon); }
.badge-afternoon { background: #14283a; color: var(--afternoon); }
.badge-evening { background: #2a1a4a; color: var(--evening); }

.task-title {
  font-size: 30rpx;
  font-weight: 600;
  margin-bottom: 6rpx;
  display: block;
  color: var(--text);
}

.task-desc {
  font-size: 26rpx;
  color: var(--text2);
  margin-bottom: 20rpx;
  line-height: 1.6;
}

.task-xp {
  font-size: 24rpx;
  color: var(--xp);
  font-weight: 500;
}

/* ═══ 反思区 ═══ */
.reflection-area {
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 2rpx solid var(--border);
}

.reflection-prompt {
  font-size: 24rpx;
  color: var(--text2);
  margin-bottom: 14rpx;
  font-style: italic;
  display: block;
}

.reflection-input {
  width: 100%;
  min-height: 120rpx;
  background: var(--surface2);
  border: 2rpx solid var(--border);
  border-radius: var(--radius-sm);
  padding: 16rpx 20rpx;
  font-size: 26rpx;
  color: var(--text);
  box-sizing: border-box;
}

.reflection-actions {
  display: flex;
  gap: 12rpx;
  margin-top: 14rpx;
}

/* ═══ 按钮 ═══ */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  padding: 18rpx 36rpx;
  border-radius: 40rpx;
  border: none;
  font-size: 26rpx;
  font-weight: 600;
  transition: all 0.2s;
  line-height: 1.2;
}

.btn::after {
  border: none;
}

.btn-complete {
  background: var(--surface2);
  color: var(--text);
  border: 2rpx solid var(--border);
  padding: 18rpx 36rpx;
  font-size: 26rpx;
}

.btn-complete:active {
  border-color: var(--xp);
  color: var(--xp);
  transform: scale(0.96);
}

.btn-save {
  background: var(--xp);
  color: #000;
  padding: 14rpx 32rpx;
  font-size: 24rpx;
  flex: 1;
}

.btn-save:active {
  background: var(--xp2);
  transform: scale(0.96);
}

.btn-undo {
  background: transparent;
  color: var(--text3);
  border: 2rpx solid var(--border);
  padding: 14rpx 24rpx;
  font-size: 24rpx;
}

/* ═══ 本周网格 ═══ */
.week-grid {
  display: flex;
  gap: 10rpx;
  padding: 0 32rpx;
  margin-bottom: 28rpx;
}

.week-day {
  flex: 1;
  text-align: center;
  padding: 18rpx 6rpx;
  background: var(--surface);
  border: 2rpx solid var(--border);
  border-radius: var(--radius-sm);
}

.week-day.today {
  border-color: var(--gold);
}

.week-day.done {
  border-color: var(--xp);
  background: #0f1a17;
}

.week-day-name {
  font-size: 22rpx;
  color: var(--text);
}

.week-day-date {
  font-size: 20rpx;
  color: var(--text3);
  display: block;
  margin-top: 4rpx;
}

.dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  display: inline-block;
  margin-top: 8rpx;
}

.dot.all-done { background: var(--xp); }
.dot.partial { background: var(--gold); }
.dot.none { background: var(--border); }

/* ═══ 历史记录 ═══ */
.history-view {
  min-height: 400rpx;
}

.history-empty {
  color: var(--text3);
  text-align: center;
  padding: 64rpx 32rpx;
  font-size: 28rpx;
}

.history-item {
  margin: 0 32rpx 16rpx;
  background: var(--surface);
  border: 2rpx solid var(--border);
  border-radius: var(--radius-sm);
  padding: 24rpx;
}

.history-date {
  font-weight: 600;
  font-size: 28rpx;
  color: var(--text);
  display: block;
}

.history-summary {
  font-size: 24rpx;
  color: var(--text2);
  margin-top: 8rpx;
  display: block;
}

.history-tags {
  display: flex;
  gap: 8rpx;
  margin-top: 12rpx;
  flex-wrap: wrap;
}

.history-tag {
  font-size: 22rpx;
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
}

.tag-all-done { background: #0f1a17; color: var(--xp); }
.tag-eng { background: #14283a; color: var(--afternoon); }
.tag-agent { background: #2a1a4a; color: var(--evening); }
.tag-fail { background: #3a1a1a; color: #e0556a; }

/* ═══ 底部导航 ═══ */
.nav {
  display: flex;
  gap: 14rpx;
  padding: 0 32rpx;
  margin-top: 36rpx;
}

.nav-btn {
  flex: 1;
  text-align: center;
  padding: 22rpx 14rpx;
  background: var(--surface);
  border: 2rpx solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--text2);
  font-size: 26rpx;
  transition: all 0.2s;
}

.nav-btn::after { border: none; }

.nav-btn.active {
  border-color: var(--gold);
  color: var(--gold);
}

/* ═══ 重置 ═══ */
.reset-area {
  margin-top: 48rpx;
  padding-bottom: 60rpx;
  text-align: center;
}

.btn-reset {
  background: transparent;
  border: 2rpx solid var(--border);
  color: var(--text3);
  padding: 12rpx 32rpx;
  border-radius: 32rpx;
  font-size: 22rpx;
  line-height: 1.2;
}

.btn-reset::after { border: none; }

/* ════════════════════════════════════
   入场动画 — GSAP 级缓动曲线
   GSAP cubic-bezier 等价：
   - power2.out:  cubic-bezier(0.215, 0.61, 0.355, 1)
   - power3.out:  cubic-bezier(0.165, 0.84, 0.44, 1)
   - power4.out:  cubic-bezier(0.23, 1, 0.32, 1)
   - back.out(1.7): cubic-bezier(0.175, 0.885, 0.32, 1.275)
   - back.inOut:   cubic-bezier(0.68, -0.55, 0.265, 1.55)
   - expo.out:     cubic-bezier(0.19, 1, 0.22, 1)
   - elastic.out:  cubic-bezier(0.16, 1.36, 0.48, 0.99)
   ════════════════════════════════════ */

/* ── 统计卡片弹入 (back.out — 超出再回弹) ── */
.stats-enter {
  animation: statsSlideIn 0.55s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
}

@keyframes statsSlideIn {
  from { opacity: 0; transform: translateY(60rpx) scale(0.85); }
  to   { opacity: 1; transform: translateY(0) scale(1); }
}

/* ── XP 进度条滑入 (power3.out — 快速减速) ── */
.xp-enter {
  animation: xpSlideIn 0.6s cubic-bezier(0.165, 0.84, 0.44, 1) forwards;
}

@keyframes xpSlideIn {
  from { opacity: 0; transform: translateX(-40rpx); }
  to   { opacity: 1; transform: translateX(0); }
}

/* ── 身份横幅弹出 (back.inOut — 先回缩再弹出) ── */
.identity-enter {
  animation: identityPop 0.55s cubic-bezier(0.68, -0.55, 0.265, 1.55) forwards;
}

@keyframes identityPop {
  from { opacity: 0; transform: scale(0.8); }
  to   { opacity: 1; transform: scale(1); }
}

/* ── 任务卡片 stagger 进入 (power4.out — 强力缓出) ── */
.card-enter {
  animation: cardSlideUp 0.5s cubic-bezier(0.23, 1, 0.32, 1) forwards;
}

@keyframes cardSlideUp {
  from {
    opacity: 0;
    transform: translateY(60rpx) scale(0.94);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* ── 完成打卡时的脉冲反馈 ── */
.task-card.pulse-once {
  animation: taskPulse 0.45s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes taskPulse {
  0%   { transform: scale(1); }
  40%  { transform: scale(1.03); }
  100% { transform: scale(1); }
}

/* ── 卡片按压反馈 (power2.in — 快速响应) ── */
.task-card:active {
  transform: scale(0.96);
  transition: transform 0.12s cubic-bezier(0.55, 0.055, 0.675, 0.19);
}

/* ── 任务标题行 ── */
.task-title-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.task-arrow {
  font-size: 40rpx;
  color: var(--text3);
  font-weight: 300;
  transition: transform 0.22s cubic-bezier(0.215, 0.61, 0.355, 1);
}

.task-card:active .task-arrow {
  transform: translateX(8rpx);
  color: var(--gold);
}

/* ════════════════════════════════════
   XP 进度条光晕 — 多层光效
   ════════════════════════════════════ */
.xp-bar-inner {
  position: relative;
  overflow: hidden;
}

.xp-bar-inner::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255,255,255,0.15) 20%,
    rgba(255,255,255,0.35) 50%,
    rgba(255,255,255,0.15) 80%,
    transparent 100%
  );
  animation: xpShimmer 2.2s cubic-bezier(0.215, 0.61, 0.355, 1) infinite;
}

@keyframes xpShimmer {
  0%   { transform: translateX(-120%); }
  60%  { transform: translateX(40%); }
  100% { transform: translateX(120%); }
}

/* 光晕暂停时仍然可见（升级时触发） */
.xp-bar-inner.glow-pulse {
  animation: glowPulse 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes glowPulse {
  0%   { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0); }
  40%  { box-shadow: 0 0 20rpx 4rpx rgba(0, 212, 170, 0.5); }
  100% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0); }
}

/* ════════════════════════════════════
   Toast 动画 — GSAP 弹性级
   ════════════════════════════════════ */
.toast {
  position: fixed;
  top: 120rpx;
  left: 50%;
  transform: translateX(-50%);
  padding: 26rpx 44rpx;
  border-radius: 48rpx;
  font-weight: 700;
  font-size: 28rpx;
  z-index: 999;
  pointer-events: none;
  animation: toastBounce 0.55s cubic-bezier(0.16, 1.36, 0.48, 0.99) forwards;
}

@keyframes toastBounce {
  0%   { opacity: 0; transform: translateX(-50%) translateY(-80rpx) scale(0.7); }
  50%  { opacity: 1; transform: translateX(-50%) translateY(12rpx) scale(1.04); }
  70%  { transform: translateX(-50%) translateY(-4rpx) scale(0.98); }
  100% { opacity: 1; transform: translateX(-50%) translateY(0) scale(1); }
}

/* Toast 消失动画 */
.toast.toast-exit {
  animation: toastExit 0.3s cubic-bezier(0.55, 0, 1, 0.45) forwards;
}

@keyframes toastExit {
  from { opacity: 1; transform: translateX(-50%) translateY(0) scale(1); }
  to   { opacity: 0; transform: translateX(-50%) translateY(-30rpx) scale(0.85); }
}

.toast.crit {
  background: var(--gold);
  color: #000;
  animation-name: toastCritBounce;
}

@keyframes toastCritBounce {
  0%   { opacity: 0; transform: translateX(-50%) translateY(-80rpx) scale(0.5) rotate(-5deg); }
  50%  { opacity: 1; transform: translateX(-50%) translateY(12rpx) scale(1.08) rotate(2deg); }
  70%  { transform: translateX(-50%) translateY(-6rpx) scale(0.96) rotate(-1deg); }
  100% { opacity: 1; transform: translateX(-50%) translateY(0) scale(1) rotate(0deg); }
}

.toast.normal {
  background: var(--xp);
  color: #000;
}

.toast.streak-bonus {
  background: var(--streak);
  color: #fff;
}

.toast.level-up {
  background: linear-gradient(135deg, #f0b90b, #ff8c00);
  color: #000;
  font-size: 32rpx;
  animation-name: toastLevelUp;
  animation-duration: 0.7s;
}

@keyframes toastLevelUp {
  0%   { opacity: 0; transform: translateX(-50%) translateY(-100rpx) scale(0.3); }
  55%  { opacity: 1; transform: translateX(-50%) translateY(10rpx) scale(1.1); }
  70%  { transform: translateX(-50%) translateY(-8rpx) scale(0.95); }
  85%  { transform: translateX(-50%) translateY(4rpx) scale(1.02); }
  100% { opacity: 1; transform: translateX(-50%) translateY(0) scale(1); }
}

/* ════════════════════════════════════
   主题切换过渡
   ════════════════════════════════════ */
.theme-toggle {
  transition: all 0.28s cubic-bezier(0.215, 0.61, 0.355, 1);
}

/* 统计数字变化动画 */
.stat-value {
  transition: color 0.3s cubic-bezier(0.215, 0.61, 0.355, 1);
}

.stat-value.pop {
  animation: statPop 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes statPop {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.25); }
  100% { transform: scale(1); }
}

/* ── 本周网格日标记过渡 ── */
.week-day {
  transition: all 0.25s cubic-bezier(0.215, 0.61, 0.355, 1);
}

.dot {
  transition: background 0.3s cubic-bezier(0.215, 0.61, 0.355, 1);
}

/* ── 按钮统一过渡 ── */
.btn, .btn-complete, .btn-save, .btn-undo, .nav-btn {
  transition: all 0.22s cubic-bezier(0.215, 0.61, 0.355, 1);
}

/* ── 反思区展开动画 ── */
.reflection-area {
  animation: reflectExpand 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
}

@keyframes reflectExpand {
  from { opacity: 0; max-height: 0; margin-top: 0; padding-top: 0; }
  to   { opacity: 1; max-height: 500rpx; margin-top: 20rpx; padding-top: 20rpx; }
}

/* ── 历史记录列表进入 ── */
.history-item {
  animation: historyFadeUp 0.35s cubic-bezier(0.23, 1, 0.32, 1) forwards;
}

@keyframes historyFadeUp {
  from { opacity: 0; transform: translateY(24rpx); }
  to   { opacity: 1; transform: translateY(0); }
}
